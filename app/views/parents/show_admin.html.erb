<% provide(:title, "Dashboard") %>
<div class="jumbotron">
  <h1><%= "#{@parent.first_name} #{@parent.last_name}" %>'s Admin Dashboard</h1>
  <p class="lead">Manage users of the Infant Passport app.</p>

</div>
<h2>Users: </h2>
<%= will_paginate %>
<table class="table table-hover">
  <tbody>
    <%= render @parents %>
  </tbody>
</table>
<%= will_paginate %>
